# Voice Notes – Speech to Text

This project converts spoken voice into text using Python and automatically saves it as notes with a timestamp.

Technologies: Python, SpeechRecognition, PyAudio

Run the project

pip install -r requirements.txt  
python src/main.py
